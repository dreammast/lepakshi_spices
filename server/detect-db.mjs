import mysql from "mysql2/promise"; import { readFileSync } from "fs"; import { join, dirname } from "path"; import { fileURLToPath } from "url"; const __dirname = dirname(fileURLToPath(import.meta.url)); const conn = await mysql.createConnection({ host: "gateway01.ap-southeast-1.prod.aws.tidbcloud.com", port: 4000, user: "2PTTBfwEwemLPuv.root", password: "Baj7n2Ec81sAYiip", database: "lepakshispices", ssl: { ca: readFileSync(join(__dirname, "certs/tidb-ca.pem")) } }); const tabs = ["wholesale_orders","wholesale_order_items","wholesale_invoices","wholesale_activity_log"]; for (const t of tabs) { const [r] = await conn.execute("SELECT COUNT(*) as c FROM information_schema.tables WHERE table_schema=? AND table_name=?",["lepakshispices",t]); console.log("Table " + t + ": " + (r[0].c > 0 ? "EXISTS" : "NOT FOUND")); } const cols = [["quotations","revision_number"],["quotations","parent_quotation_id"],["quotations","additional_charges"],["quotations","delivery_terms"]]; for (const [t,c] of cols) { const [r] = await conn.execute("SELECT COUNT(*) as cnt FROM information_schema.columns WHERE table_schema=? AND table_name=? AND column_name=?",["lepakshispices",t,c]); console.log("Column " + t + "." + c + ": " + (r[0].cnt > 0 ? "EXISTS" : "NOT FOUND")); } await conn.end(); console.log("Done.");
